// Commented out for future use
/*
import { impactMetrics } from '../../data/impact';

export function Impact() {
  return (
    <section className="mb-24 bg-gray-50 py-16 px-8 rounded-2xl">
      <h2 className="text-4xl font-bold mb-12 text-center">Our Impact</h2>
      <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {impactMetrics.map((impact, index) => (
          <div key={index} className="text-center">
            <div className="text-4xl font-bold text-brand mb-2">{impact.metric}</div>
            <div className="text-xl font-semibold mb-2">{impact.label}</div>
            <p className="text-gray-600">{impact.description}</p>
          </div>
        ))}
      </div>
    </section>
  );
}
*/